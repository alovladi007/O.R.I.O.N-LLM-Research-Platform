# Core dependencies
pandas>=2.0.0
numpy>=1.24.0

# AI/ML dependencies (LangChain stack retained for the Phase 7 agent work.
# RAG-specific extras like langchain-community + faiss-cpu + sentence-transformers
# were removed with the legacy src/rag package in Session 0.2 and will be
# re-added, scoped, in Phase 7 if actually used.)
openai>=1.0.0
anthropic>=0.25.0
langchain>=0.1.0
langchain-openai>=0.0.5
torch>=2.0.0
torch-geometric>=2.3.0
transformers>=4.30.0
scikit-learn>=1.3.0
scikit-fem>=12.0.0                 # Phase 5 / Session 5.1 FEM continuum solver
meshio>=5.3.0                      # VTU / .msh IO for continuum solver
matminer>=0.9.0                    # Phase 6 / Session 6.1 ML featurizers (composition + SiteStatsFingerprint)
# xgboost 3.x on macOS + anaconda ships against a system libomp that
# triggers ``Symbol not found: ___kmpc_dispatch_deinit`` on import.
# Upper-cap at <3 until the linux-CI path picks up xgboost 3 and we
# can validate. Sessions 6.3+ use quantile regression, which is
# well-supported in both 2.x and 3.x.
xgboost>=2.0.0,<3.0.0
mlflow>=2.16.0                     # Phase 6 / Session 6.3 training-run tracking
pytorch-lightning>=2.0.0,<3.0.0    # Phase 6 / Session 6.4 CGCNN training loop
torchmetrics>=1.0.0                # Phase 6 / Session 6.4 standard metric logging
botorch>=0.10.0,<0.13.0            # Phase 7 / Session 7.1 Bayesian optimization (pulls in gpytorch + linear-operator)

# Optional (not pinned here):
#   mp-api<0.45 + emmet-core<0.85 — for scripts/orion_fetch_mp_oxides.py.
# emmet-core>=0.85 uses typing.NotRequired which requires Python 3.11;
# we stay on Python 3.10 for the canonical CI image, so the script
# imports mp_api lazily and callers who need it install it themselves
# via ``pip install 'mp-api<0.45' 'emmet-core<0.85'``. Moving to
# Python 3.11 in a future deploy-infra session drops the upper pin.

# Database and storage (Neo4j removed in Session 0.2 with the legacy
# knowledge_graph package; MongoDB removed because nothing in the canonical
# codebase uses it.)
redis>=5.0.0
sqlalchemy>=2.0.0
aioredis>=2.0.0
asyncpg>=0.29.0
alembic>=1.12.0
pgvector>=0.2.0
psycopg2-binary>=2.9.0

# Graph utilities (networkx kept for DAG / workflow graph computations in
# backend/common/workflows; RAG libraries rdflib + owlready2 removed.)
networkx>=3.0

# Materials science
pymatgen>=2023.5.0
ase>=3.22.0
rdkit>=2023.3.1

# API and web
fastapi>=0.100.0
uvicorn>=0.23.0
httpx>=0.24.0
aiohttp>=3.8.0
pydantic[email]>=2.0.0       # email-validator needed by pydantic's EmailStr (auth schemas)
pydantic-settings>=2.0.0
python-multipart>=0.0.6      # FastAPI form/file uploads
itsdangerous>=2.1.0          # starlette.middleware.sessions.SessionMiddleware

# Auth (split out from pyproject.toml into the runtime list; these
# are imported from src.api.auth.security on every request path).
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
pyjwt>=2.8.0

# Object storage (MinIO, used by the artifacts + bulk-import pipelines).
minio>=7.1.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0
jinja2>=3.1.0
tqdm>=4.65.0
psutil>=5.9.0
gputil>=1.4.0
pint>=0.22                         # SI unit arithmetic (backend.common.units)

# Task Queue / Workers
celery>=5.3.0
kombu>=5.3.0
flower>=2.0.0  # Celery monitoring

# Monitoring / Tracing
prometheus-client>=0.17.0
prometheus-fastapi-instrumentator>=6.1.0
slowapi>=0.1.9
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-jaeger>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0